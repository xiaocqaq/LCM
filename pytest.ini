[pytest]
# Historical standalone scripts can read credentials and write to production on import.
testpaths = tests/reliability
python_files = test_*.py
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    integration: requires an explicitly configured disposable PostgreSQL database
